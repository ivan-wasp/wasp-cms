
ng build --configuration development --aot --output-hashing=all
npm run build

ng add @angular/material
npm i @swimlane/ngx-charts --save

npm install --save @angular/material-moment-adapter moment

npm install ion2-calendar@next moment --save https://github.com/shaneparsons/ion2-calendar#76ae1e241183500a377647b363c931a03aa50bca
ionic 5 icon problem

<!-- REMARK add too tsconfig.app.json
  "compilerOptions": {
    "allowSyntheticDefaultImports": true
  }, -->

npm install --save @angular-material-components/datetime-picker

npm install ngx-color-picker --save

npm install ionic2-calendar --save

npm install @swimlane/ngx-charts --save

ion-searchable problem renderer2
// this._renderer.setElementClass(this._ionItemElement, cssClass, shouldAdd);
shouldAdd ? this._renderer.addClass(this._ionItemElement, cssClass) : this._renderer.removeClass(this._ionItemElement, cssClass)

npm install --save signature_pad

